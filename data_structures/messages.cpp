#include "messages.hpp"

// This translation unit is intentionally minimal.
// It exists to mirror the ABP repository structure where custom message
// types are compiled into a separate object file.
